<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="pos_technotrade_product_normal_form_view" model="ir.ui.view">
        <field name="name">pos_technotrade.product.product.form</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_normal_form_view"/>
        <field name="arch" type="xml">
            <field name="barcode" position="after">
                <field name="nozzle_ids" widget="many2many_tags"/>
            </field>
        </field>
    </record>

</odoo>
